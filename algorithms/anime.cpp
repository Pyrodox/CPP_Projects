#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
    ll n, k;
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> n >> k;
    for (ll i = 0; i < k; ++i) {
        ll a, b;
        cin >> a >> b;
    }
    for (ll i = 0; i < n; ++i) {
        cout << 1 << " ";
    }
}